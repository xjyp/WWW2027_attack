
% Introduction 分段规划（每段一句话概括）

% 第一段（背景与重要性）：
% 介绍成对偏好评估（pairwise preference evaluation）在 LLM 评估与对齐（Chatbot Arena、RLHF、DPO）中的核心作用
% 第二段（现有攻击与研究缺口）：
% 指出现有针对 LLM 偏好判断的攻击可分为回复级操纵和提示级操纵，
% 但二者都依赖较强的攻击者能力假设，尚不清楚在无法访问核心评估内容、仅能修改有限外部输入的严格受限条件下，偏好操纵是否仍然可行。

% 第三段（核心发现与中心挑战）：
% 我们发现即使在受限条件下偏好操纵依然可行,但不同攻击在不同样本子集上奏效、呈现显著的异质性与互补性, 
% 因此，受限访问下的研究问题不再局限于验证“偏好是否能够被操纵”，而进一步转向如何突破单一攻击的有限覆盖，
% 利用不同攻击之间的异质成功模式获得更广泛的偏好翻转覆盖。"

% 第四段（两个具体技术挑战）：
% 应对上述挑战需克服两个紧密关联的局限：其一，如何在受限访问条件下构建兼具有效性与行为多样性的结构化攻击空间，
% 使不同候选能够诱发差异化的偏好翻转模式，而非仅生成大量形式不同但作用高度重叠的攻击；
% 其二，有限查询预算下需要利用细粒度边际反馈来区分互补候选与冗余候选，而单一候选的成功率无法捕捉这种互补性。

% 第五段（本文方法 PDA-KAS）：
% 为此，我们在输入不可见、注入位置受限的设定下提出 PDA-KAS——一个将因子化攻击空间与细粒度覆盖感知相结合的结构化搜索框架，
% 在有限查询预算内系统探索多样攻击策略并识别互补组合。

% 第六段（贡献总结，可用 itemize）：
% 总结本文三项贡献：(1) 构建结构化、内容无关的因子化攻击空间，兼顾多样性、有效性与语义自然性；
% (2) 提出细粒度、覆盖感知的评估机制，以更少的查询优先发现贡献新翻转的互补策略；
% (3) 开展受限访问条件下偏好操纵的系统性实证研究，在多种数据集与评判模型上评估 PDA-KAS，
% 并通过攻击位置、任务类型、策略组成与覆盖增益等分析，揭示受限偏好操纵的有效性边界及其异质成功模式.








% \begin{figure}[t]
%   \centering
%   \includegraphics[width=\textwidth]{sampleteaser.pdf}
% %   \vspace{-2em}
%   \caption{.}
%   \label{fig:intro-fig}
% %   \vspace{-1em}
% \end{figure}
